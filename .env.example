# Environment variables for testing scripts
# Copy this file to .env and fill in your actual values
# Note: These are only used for local testing scripts, NOT for the deployed worker

# Your deployed worker URL (get this after running 'npm run deploy')
WEBHOOK_URL=https://your-worker.workers.dev/webhook

# The same auth token you set with 'wrangler secret put WEBHOOK_AUTH_TOKEN'
WEBHOOK_AUTH_TOKEN=your-webhook-auth-token-here

# Example: Generate a secure token with:
# openssl rand -base64 32
